[project]
mapping = "src:.build"
# after_build_command = "wranger pages deploy .build --branch production"

#[plugins.minify]
#suffixes = [".js", ".css"]

#[plugins.minify.methods]
#js = "external"
#css = "native"

[plugins.sass]
mapping = "sass:css"

[plugins.typescript]
mapping = "ts:js"
